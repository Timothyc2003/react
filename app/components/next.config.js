/** @type {import('next').NextConfig} */
const nextConfig = {
    // Enable if using static export (uncomment if needed)
    // output: 'export', // Only for fully static sites (no API routes)
  };
  
  module.exports = nextConfig;